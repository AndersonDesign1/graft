## What changed

<!-- What this does, and the problem it solves. One paragraph. -->

## Why

<!-- Link the issue, or say what went wrong without this. -->

## Breaking?

<!-- Graft is pre-1.0, so a break that buys a better design is welcome.
     Say what breaks and what a user has to change. Write "No" if nothing does. -->

## How you tested it

<!-- Name the command. "Ran the tests" is not a test report. -->

```

```

## Checklist

- [ ] `pnpm format:check`, `pnpm lint`, and `pnpm typecheck` pass
- [ ] `pnpm test` passes
- [ ] Added or updated a test that fails without this change
- [ ] `pnpm changeset` run, if this touches a published package
- [ ] Any new error carries a `fix` an agent can act on
- [ ] Docs updated, if this changes a documented surface
