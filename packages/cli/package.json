{
  "name": "@graft/cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "graft": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@graft/assets": "workspace:*",
    "@graft/compiler": "workspace:*",
    "@graft/content-migrations": "workspace:*",
    "@graft/contracts": "workspace:*",
    "@graft/core": "workspace:*",
    "@graft/db": "workspace:*",
    "jiti": "^2.4.0"
  }
}
